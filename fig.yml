db:
  image: postgres:9.3
  ports:
    - "5432"

redis:
  image: redis:2.8
  ports:
    - "6379"

web:
  build: .
  command: bundle exec foreman start 
  ports:
    - "3000:3000"
  links:
    - db
    - redis
